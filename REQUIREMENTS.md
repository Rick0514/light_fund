# 基金持仓管理工具 - 需求文档

## 1. 项目概述

一个基于 Web 的前后台程序，用于管理个人基金的持仓操作记录。前端通过 HTML 页面交互，后端以 Python 提供 API 服务，底层调用已实现的 `get_fund_info()` 获取基金实时净值数据。

## 2. 功能需求

### 2.1 自选基金管理
- **添加自选基金**：输入基金代码（如 `110003`），系统拉取该基金最新净值和当日涨跌，加入自选列表
- **自选基金列表**：展示所有已添加的自选基金，包含：
  - 基金代码
  - 基金名称
  - 最新单位净值
  - 当日涨跌幅
  - 净值日期
  - 累计投入金额
  - 当前市值（累计份额 × 最新净值）
  - 持仓盈亏（当前市值 - 累计投入）
- **删除自选基金**：从列表中移除（关联的加仓记录一并删除）

### 2.2 加仓记录管理

#### 2.2.1 入口与交互
- 每个基金卡片底部有 **「加仓信息」** 按钮，点击后在卡片内展开加仓面板
- 加仓面板包含两部分：**加仓记录列表** + **添加加仓表单**

#### 2.2.2 添加加仓
- **选择日期**：加仓日期（日期选择器，默认当天）
- **输入金额**：加仓金额（元）
- **输入备注**：可选备注
- 提交时，系统按加仓日期的**当日净值**自动计算份额：
  - 份额 = 加仓金额 / 当日净值（保留 4 位小数）
- 如果加仓日期为非交易日，系统向前回退取最近交易日净值

#### 2.2.3 加仓记录列表
- 展示该基金所有加仓记录，每条包含：
  - 加仓日期
  - 金额（元）
  - 当日净值
  - 折算份额
  - 备注
- 支持删除单条加仓记录

#### 2.2.4 持仓汇总（显示在加仓面板底部）
- **累计投入**：所有加仓金额之和
- **累计份额**：所有加仓折算份额之和
- **当前市值**：累计份额 × 最新单位净值
- **持仓盈亏**：当前市值 - 累计投入

#### 2.2.5 基金卡片上的持仓信息
- 卡片上始终显示该基金的累计投入、当前市值、持仓盈亏（无需展开面板即可看到）

### 2.3 数据持久化
- 自选基金列表和加仓记录使用 **JSON 文件** 持久化存储
- 程序重启后数据不丢失

## 3. 技术栈

| 层 | 技术 | 说明 |
|---|---|---|
| 后端 | Python 3.8+ | 标准库为主，轻量 HTTP 服务 |
| Web 框架 | `http.server` (标准库) | 无需额外安装依赖 |
| 前端 | Pico.css + Alpine.js | CDN 引入，零安装 |
| 数据存储 | JSON 文件 | `data/funds.json` |
| 基金数据 | 东方财富 API | `fund_api.py` |

## 4. 项目结构

```
proj1/
├── fund_api.py           # 基金数据 API 封装
├── server.py             # HTTP 服务主入口
├── test_fund.py          # 基金净值查询测试
├── data/
│   └── funds.json        # 持久化数据文件
├── static/
│   ├── index.html        # 前端页面
│   ├── style.css         # 样式
│   ├── app.js            # 前端交互逻辑
│   ├── pico.min.css      # Pico CSS
│   └── alpine.min.js     # Alpine.js
└── REQUIREMENTS.md       # 本需求文档
```

## 5. API 设计

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/funds` | 获取自选基金列表（含实时净值 + 持仓汇总） |
| POST | `/api/funds` | 添加自选基金 |
| DELETE | `/api/funds/<code>` | 删除自选基金（含关联加仓记录） |
| GET | `/api/funds/<code>/positions` | 获取某基金的加仓记录及持仓汇总 |
| POST | `/api/funds/<code>/positions` | 添加加仓记录 `{date, amount, note}` |
| DELETE | `/api/funds/<code>/positions/<id>` | 删除单条加仓记录 |

## 6. 数据模型

```json
{
  "funds": {
    "110003": {
      "name": "易方达上证50指数A",
      "added_at": "2026-06-20",
      "positions": [
        {
          "id": "a1b2c3d4",
          "date": "2026-06-18",
          "amount": 10000.00,
          "nav": 1.8252,
          "shares": 5478.85,
          "note": ""
        }
      ]
    }
  }
}
```

## 7. 界面布局

```
┌──────────────────────────────────────────────────────────┐
│  📊 基金持仓管理                                          │
│  自选基金 · 净值追踪                                       │
├──────────────────────────────────────────────────────────┤
│  输入基金代码 [__________] [+ 添加自选]                    │
├──────────────────────────────────────────────────────────┤
│  自选基金列表                                              │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ 110003  易方达上证50指数A                   [删除]   │ │
│  │ 净值: 1.8252  涨跌: +0.23%  日期: 2026-06-20        │ │
│  │ 累计投入: ¥10,000  市值: ¥10,115  盈亏: +¥115       │ │
│  │                                   [加仓信息 ▼]       │ │
│  ├──────────────────────────────────────────────────────┤ │
│  │  加仓记录                                            │ │
│  │  ┌──────────┬─────────┬────────┬─────────┬────┬──┐  │ │
│  │  │   日期   │  金额   │  净值  │  份额   │备注│删除│ │ │
│  │  ├──────────┼─────────┼────────┼─────────┼────┼──┤  │ │
│  │  │2026-06-18│¥10,000  │ 1.8252 │ 5478.85 │ -- │ ✕ │  │ │
│  │  └──────────┴─────────┴────────┴─────────┴────┴──┘  │ │
│  │  累计投入: ¥10,000  累计份额: 5478.85                 │ │
│  │  当前市值: ¥10,115  盈亏: +¥115                       │ │
│  │                                                       │ │
│  │  添加加仓:                                            │ │
│  │  [日期选择] [金额____] [备注____] [添加]              │ │
│  └──────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

## 8. 核心逻辑说明

### 8.1 份额计算
```
份额 = 加仓金额 / 加仓日期当日净值
```
- 净值取加仓日期当天的单位净值
- 加仓日期为非交易日时，向前回退取最近交易日净值
- 份额保留 4 位小数

### 8.2 持仓汇总
```
累计投入   = Σ(每条加仓金额)
累计份额   = Σ(每条加仓份额)
当前市值   = 累计份额 × 最新单位净值
持仓盈亏   = 当前市值 - 累计投入
```

### 8.3 卡片持仓数据刷新
- 基金列表加载时，同时计算每个基金的持仓汇总
- 最新净值通过 `get_fund_info()` 实时获取
- 刚添加加仓后立即刷新列表

## 9. 实现步骤

1. 在 `server.py` 中新增 2.2 涉及的 API 路由
2. 更新 `funds.json` 数据读写兼容 `positions` 字段
3. 更新 `index.html` 添加加仓面板 UI
4. 更新 `app.js` 添加加仓交互逻辑
5. 更新 `style.css` 添加加仓面板样式
6. 联调测试
