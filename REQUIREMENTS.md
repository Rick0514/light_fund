# 基金持仓管理工具 - 需求文档

## 1. 项目概述

一个基于 Web 的前后台程序，用于管理个人基金的持仓操作记录。前端通过 HTML 页面交互，后端以 Python 提供 API 服务，底层调用已实现的 `get_fund_info()` 获取基金实时净值数据。

## 2. 功能需求

### 2.1 自选基金管理
- **添加自选基金**：输入基金代码（如 `110003`），系统拉取该基金最新净值和当日涨跌，加入自选列表
- **自选基金列表**：展示所有已添加的自选基金，包含：
  - 基金代码
  - 基金名称（可选，从接口获取）
  - 最新单位净值
  - 当日涨跌幅
  - 数据更新时间
- **删除自选基金**：从列表中移除

### 2.2 数据持久化
- 自选基金列表使用 **JSON 文件** 持久化存储
- 程序重启后数据不丢失

## 3. 技术栈

| 层 | 技术 | 说明 |
|---|---|---|
| 后端 | Python 3.8+ | 标准库为主，轻量 HTTP 服务 |
| Web 框架 | `http.server` (标准库) | 无需额外安装依赖 |
| 前端 | 原生 HTML + CSS + JS | 单页面应用，Ajax 交互 |
| 数据存储 | JSON 文件 | `data/funds.json` 存储自选列表 |
| 基金数据 | 东方财富 API | 复用 `test_fund.py` 中的 `get_fund_info()` |

## 4. 项目结构

```
proj1/
├── test_fund.py          # 基金净值查询（已完成）
├── fund_api.py           # 基金数据 API 封装（净值 + 名称获取）
├── server.py             # HTTP 服务主入口
├── data/
│   └── funds.json        # 持久化数据文件
├── static/
│   ├── index.html        # 前端页面
│   ├── style.css         # 样式
│   └── app.js            # 前端交互逻辑
└── REQUIREMENTS.md       # 本需求文档
```

## 5. API 设计

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/funds` | 获取自选基金列表（含当日净值） |
| POST | `/api/funds` | 添加自选基金 `{fund_code: "110003"}` |
| DELETE | `/api/funds/<code>` | 删除自选基金 |

## 6. 数据模型

```json
{
  "funds": {
    "110003": {
      "name": "易方达上证50指数A",
      "added_at": "2025-06-22"
    }
  }
}
```

## 7. 界面布局

```
┌─────────────────────────────────────────────┐
│  基金持仓管理                                │
├─────────────────────────────────────────────┤
│  添加自选基金: [____基金代码____] [添加]     │
├─────────────────────────────────────────────┤
│  自选基金列表                                │
│  ┌─────────────────────────────────────────┐ │
│  │ 110003  易方达上证50指数A               │ │
│  │ 净值: 1.8252  涨跌: +0.23%  [删除]     │ │
│  ├─────────────────────────────────────────┤ │
│  │ ...                                     │ │
│  └─────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

## 8. 实现步骤

1. 创建 `fund_api.py` —— 封装基金数据获取（净值 + 基金名称）
2. 创建 `server.py` —— HTTP 服务 + API 路由 + JSON 数据读写
3. 创建前端页面 `index.html` + `style.css` + `app.js`
4. 联调测试
